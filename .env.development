# Nome do projeto (será substituído automaticamente pelo script)
# Formato recomendado: letras minúsculas, números e hífens
# Exemplos: meu-blog, loja-virtual, portfolio
COMPOSE_PROJECT_NAME=default

# Portas (serão substituídas automaticamente pelo script)
# As portas são calculadas com base no nome do projeto
# para evitar conflitos entre múltiplas instâncias
WORDPRESS_PORT=8000  # Porta base + offset do projeto
MYSQL_PORT=3306      # Porta base + offset do projeto
ADMINER_PORT=8080    # Porta base + offset do projeto

# Configurações do MySQL
# IMPORTANTE: Use senhas fortes em produção!
MYSQL_ROOT_PASSWORD=somewordpress
MYSQL_DATABASE=wordpress
MYSQL_USER=wordpress
MYSQL_PASSWORD=wordpress

# Configurações do WordPress
# Estas credenciais devem corresponder às do MySQL acima
WORDPRESS_DB_NAME=wordpress
WORDPRESS_DB_USER=wordpress
WORDPRESS_DB_PASSWORD=wordpress

# Limites de memória do MySQL
# Ajuste conforme necessário para seu ambiente
MYSQL_MEMORY_LIMIT=1g          # Limite máximo de memória
MYSQL_MEMORY_RESERVATION=500m  # Memória reservada
